server:
  host: "0.0.0.0"
  port: 8182

# Optional: snapshot the in-memory cache to disk so restarts start from the
# last snapshot instead of empty. Remove the block for a purely in-memory cache.
# cache:
#   persistence:
#     path: "/var/lib/unified-api/cache.json"
#     interval_seconds: 60
